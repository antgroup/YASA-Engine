# YASA MCP Server - development dependencies
# Install: pip install -r requirements-dev.txt
# (includes all runtime deps from requirements.txt)

-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
