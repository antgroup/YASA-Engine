const { cloneWithDepth } = require('../../../../../util/clone-util')
const { moveExistElementsToBuffer } = require('./buffer')

/**
 * java.util.Collection
 */
class Collection {
  /**
   * Constructor
   * @param _this
   * @param argvalues
   * @param state
   * @param node
   * @param scope
   * @constructor
   */
  static Collection(_this, argvalues, state, node, scope) {
    return _this
  }
}

module.exports = Collection
