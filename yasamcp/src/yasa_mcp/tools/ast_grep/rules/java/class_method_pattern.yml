name: class_method_pattern
rule:
  all:
    - any:
        - kind: method_declaration
        - kind: constructor_declaration
    - inside:
        all:
          - any:
              - kind: enum_declaration
              - kind: class_declaration
              - kind: interface_declaration
          - has:
              kind: identifier
              regex: "^{class_name}$"
        stopBy: end
