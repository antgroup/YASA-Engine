# ============================================================================
# YASA-Engine [OSS26] MCP 统一运行底座脚手架 —— 依赖清单
# ----------------------------------------------------------------------------
# 安装方式（在项目根目录 yasa_mcp 所在处执行）：
#   pip install -r yasa_mcp/requirements.txt
#
# 说明：fastmcp 会自动带上一系列传递依赖（mcp / pydantic / uvicorn 等），
#       下方显式声明核心依赖，便于阅读、锁定与后续版本管理。
# ============================================================================

# ---- 核心：MCP 服务框架（3.x 起使用 http_app / custom_route 新 API）----
fastmcp>=3.0.0,<4.0.0

# ---- 显式声明的核心依赖 ----
click>=8.1.0            # 命令行参数解析（--transport / --port / --log-level）
pydantic>=2.5.0         # 工具入参类型校验 / 非空校验
uvicorn>=0.27.0         # HTTP 模式 ASGI 运行器
starlette>=0.37.0       # HTTP 模式 ASGI 框架（/healthz 健康检查路由、CORS）

# ---- 单元测试 ----
pytest>=8.0.0           # 运行 tests/ 下的单元测试
